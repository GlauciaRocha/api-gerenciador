{
  "name": "api-autenticada-com-testes",
  "version": "1.0.0",
  "description": "API REST com autenticação JWT e testes automatizados usando Mocha, Chai e Supertest.",
  "main": "src/server.js",
  "scripts": {
    "start": "node -r dotenv/config src/server.js",
    "dev": "nodemon -r dotenv/config src/server.js",
    "test": "mocha 'tests/**/*.test.js' --timeout 5000 --exit",
    "k6": "k6 run -e BASE_URL=http://localhost:3000 tests/k6/checkout.test.js",
    "k6:report": "k6 run -e BASE_URL=http://localhost:3000 tests/k6/checkout.test.js --out json=tests/k6/results.json && node tests/k6/generateReport.js"
  },
  "keywords": [
    "api",
    "rest",
    "nodejs",
    "express",
    "jwt",
    "mocha",
    "chai"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^10.8.2",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  },
  "directories": {
    "test": "tests"
  }
}
